<!DOCTYPE html>
<html lang="en">
<head>
    <script src="script.js" defer>
        // JavaScript to resize the background dynamically
        function adjustBackground() {
            const body = document.body;
            const windowWidth = window.innerWidth;
            const windowHeight = window.innerHeight;
            body.style.backgroundSize = `${windowWidth}px ${windowHeight}px`; // Resize to match window size
        }

        // Add event listeners
        window.addEventListener('load', adjustBackground); // Adjust on page load
        window.addEventListener('resize', adjustBackground); // Adjust on window resize
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Install Ranibow Theme Extension</title>
    <style>
        /* General Body Styles */
        body {
            background: url("Ranibow.png") no-repeat center center fixed;
            background-size: 100% 100%; /* Initial size to fill window */
            color: #ffffff;
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
            overflow: hidden; /* Prevent scrollbars due to resizing */
        }

        /* Heading Styles */
        h1 {
            font-size: 2.5em;
            text-shadow: 2px 2px 5px #000000;
            margin-bottom: 20px;
        }

        /* Paragraph Styles */
        p {
            font-size: 1.2em;
            margin: 15px 0;
            max-width: 80%;
        }

        /* Button Styles */
        button {
            background-color: #ff9800;
            color: #ffffff;
            font-size: 1.2em;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            box-shadow: 2px 2px 5px #000000;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #e67e22;
        }

        /* Link Styles */
        a {
            text-decoration: none;
            margin: 20px 0;
        }

        /* Center the Installation Instructions */
        .instructions {
            text-align: left;
            background: rgba(0, 0, 0, 0.5);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 2px 2px 5px #000000;
            width: 80%;
            max-width: 600px;
            margin: 20px auto;
        }

        /* Ordered List Styles */
        ol {
            font-size: 1.1em;
            margin: 0;
            padding: 0 0 0 10px;
        }

        li {
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <h1>Ranibow Theme style for Vectaria</h1>
    <p>Click the button below to download the extension:</p>
    <a href="Ranibow.crx" download>
        <button>Download Extension</button>
    </a>
    <div class="instructions">
        <p>Installation Instructions:</p>
        <ol>
            <li>Download the file.</li>
            <li>Then you will see "Manage" on the top in Opera, click it.</li>
            <li>Then click "Install" and confirm the installation.</li>
            <li>Have fun now 🙃.</li>
        </ol>
    </div>
</body>
</html>
